{
  "name": "dsh-plugins-workspace",
  "private": true,
  "version": "0.0.0",
  "description": "Personal multi-plugin workspace for DeepSeek Harness",
  "packageManager": "pnpm@10.27.0",
  "scripts": {
    "build": "pnpm -r build",
    "check": "pnpm -r check",
    "test": "pnpm -r test",
    "verify": "pnpm -r verify"
  }
}

